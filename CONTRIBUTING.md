# Contributing

This repository is Fern config for `@indiciaosint/sdk`. The published package is generated from `https://api.indicia.app/openapi` — do not send PRs against generated client code.

- Bugs and API mismatches: open a GitHub issue with repro steps and the SDK version.
- Generation config: edit `fern/` and the workflows in `.github/workflows/`.
